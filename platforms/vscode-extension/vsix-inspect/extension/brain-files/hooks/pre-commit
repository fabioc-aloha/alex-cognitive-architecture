#!/bin/sh
# Git pre-commit hook — requires Node.js

if command -v node > /dev/null 2>&1; then
    node ".github/hooks/pre-commit.cjs"
    exit $?
fi

echo "⚠️  Node.js not found — skipping quality gates"
echo "   Install Node.js: https://nodejs.org/"
exit 0
